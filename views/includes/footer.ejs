
<!DOCTYPE html>
<footer>
    <div class="footer-container">
        <div class="footer-section about">
            <div class="logo">🌾 Farmer's Marketplace</div>
            <p>Connecting farmers directly with consumers to provide the freshest, locally-sourced produce.</p>
            <div class="social-media">
                <% const socialPlatforms = [
                    { icon: 'facebook', link: '#' },
                    { icon: 'twitter', link: '#' },
                    { icon: 'instagram', link: '#' },
                    { icon: 'linkedin', link: '#' }
                ]; %>
                <% socialPlatforms.forEach(platform => { %>
                    <a href="<%= platform.link %>" class="social-icon" aria-label="<%= platform.icon %>">
                        <i class="fab fa-<%= platform.icon %>"></i>
                    </a>
                <% }); %>
            </div>
        </div>

        <div class="footer-section links">
            <h3>Quick Navigation</h3>
            <div class="nav-grid">
                <div class="nav-column">
                    <% const navigationLinks1 = [
                        { text: 'Home', href: '/' },
                        { text: 'Buy Crops', href: '/buy' },
                        { text: 'Sell Crops', href: '/sell' }
                    ]; %>
                    <% navigationLinks1.forEach(link => { %>
                        <a href="<%= link.href %>" class="footer-link"><%= link.text %></a>
                    <% }); %>
                </div>
                <div class="nav-column">
                    <% const navigationLinks2 = [
                        { text: 'Market Prices', href: '/prices' },
                        { text: 'Profile', href: '/profile' },
                        { text: 'About Us', href: '/about' }
                    ]; %>
                    <% navigationLinks2.forEach(link => { %>
                        <a href="<%= link.href %>" class="footer-link"><%= link.text %></a>
                    <% }); %>
                </div>
            </div>
        </div>

        <div class="footer-section contact">
            <h3>Get in Touch</h3>
            <div class="contact-details">
                <div class="contact-item">
                    <i class="fas fa-envelope"></i>
                    <span>info@farmersmarketplace.com</span>
                </div>
                <div class="contact-item">
                    <i class="fas fa-phone"></i>
                    <span>+123 456 7890</span>
                </div>
                <div class="contact-item">
                    <i class="fas fa-map-marker-alt"></i>
                    <span>123 Farm Lane, Ruralville</span>
                </div>
            </div>

            <div class="newsletter-section">
                <h4>Subscribe to Our Newsletter</h4>
                <form id="newsletter-form" class="newsletter-form">
                    <div class="input-group">
                        <input 
                            type="email" 
                            name="email" 
                            placeholder="Enter your email" 
                            required 
                            aria-label="Email for newsletter subscription"
                        >
                        <button type="submit" aria-label="Subscribe to newsletter">
                            <i class="fas fa-paper-plane"></i>
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <div class="footer-bottom">
        <div class="copyright">
            &copy; <%= new Date().getFullYear() %> Farmer's Marketplace. All rights reserved.
        </div>
        <button id="back-to-top" aria-label="Scroll to top">
            <i class="fas fa-arrow-up"></i>
        </button>
    </div>

    <style>
        :root {
            --primary-teal: #20b2aa;
            --secondary-teal: #008080;
            --dark-teal: #005f73;
            --accent-green: #2ecc71;
            --background-light: #f0f4f4;
            --text-primary: #2c3e50;
            --white: #ffffff;
            --transition-speed: 0.3s;
        }

        footer {
            background: linear-gradient(135deg, var(--secondary-teal), var(--primary-teal));
            color: var(--white);
            padding: 4rem 5%;
            font-family: 'Inter', 'Segoe UI', Roboto, sans-serif;
        }

        .footer-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .footer-section {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .logo {
            font-size: 1.75rem;
            font-weight: 800;
            letter-spacing: 1.5px;
            color: var(--white);
            display: flex;
            align-items: center;
            margin-bottom: 0.5rem;
        }

        .social-media {
            display: flex;
            gap: 1rem;
            margin-top: 1rem;
        }

        .social-icon {
            color: var(--white);
            font-size: 1.5rem;
            opacity: 0.8;
            transition: all var(--transition-speed) ease;
        }

        .social-icon:hover {
            opacity: 1;
            transform: scale(1.2);
            color: var(--accent-green);
        }

        .nav-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
        }

        .nav-column {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
        }

        .footer-link {
            color: rgba(255,255,255,0.8);
            text-decoration: none;
            transition: all var(--transition-speed) ease;
            position: relative;
            padding-bottom: 0.25rem;
        }

        .footer-link:hover {
            color: var(--accent-green);
        }

        .footer-link::after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            bottom: 0;
            left: 0;
            background-color: var(--accent-green);
            transition: width var(--transition-speed) ease;
        }

        .footer-link:hover::after {
            width: 100%;
        }

        .contact-details {
            display: flex;
            flex-direction: column;
            gap: 0.75rem;
        }

        .contact-item {
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .contact-item i {
            font-size: 1.25rem;
            min-width: 1.5rem;
            text-align: center;
        }

        .newsletter-section {
            background: rgba(255,255,255,0.1);
            padding: 1.25rem;
            border-radius: 0.5rem;
        }

        .newsletter-form {
            display: flex;
            flex-direction: column;
            gap: 0.75rem;
        }

        .input-group {
            display: flex;
            border-radius: 0.5rem;
            overflow: hidden;
        }

        .input-group input {
            flex-grow: 1;
            padding: 0.75rem;
            border: none;
            background-color: var(--white);
            color: var(--text-primary);
        }

        .input-group button {
            background-color: var(--accent-green);
            color: var(--white);
            border: none;
            padding: 0.75rem 1.25rem;
            cursor: pointer;
            transition: background-color var(--transition-speed) ease;
        }

        .input-group button:hover {
            background-color: var(--secondary-teal);
        }

        .footer-bottom {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: 2rem;
            padding-top: 1.5rem;
            border-top: 1px solid rgba(255, 255, 255, 0.2);
        }

        #back-to-top {
            background: transparent;
            border: 2px solid var(--white);
            color: var(--white);
            padding: 0.5rem 1.25rem;
            border-radius: 0.5rem;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            transition: all var(--transition-speed) ease;
        }

        #back-to-top:hover {
            background: var(--white);
            color: var(--text-primary);
        }

        @media screen and (max-width: 768px) {
            .footer-container {
                grid-template-columns: 1fr;
                text-align: center;
            }

            .nav-grid {
                grid-template-columns: 1fr;
            }

            .social-media, .contact-details, .footer-bottom {
                justify-content: center;
            }
        }
    </style>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const backToTopBtn = document.getElementById('back-to-top');
            backToTopBtn.addEventListener('click', () => {
                window.scrollTo({ top: 0, behavior: 'smooth' });
            });

            const newsletterForm = document.getElementById('newsletter-form');
            newsletterForm.addEventListener('submit', (e) => {
                e.preventDefault();
                const emailInput = newsletterForm.querySelector('input[type="email"]');
                // Placeholder for newsletter submission logic
                alert(`Subscribed with email: ${emailInput.value}`);
                emailInput.value = '';
            });
        });
    </script>
</footer>
